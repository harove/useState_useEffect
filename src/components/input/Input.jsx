import React from 'react';
import './Input.css';

/**
 * Input
 * @version v1.0.0
 * 
 * @param {*} props 
 */
const Input = (props) => {

    return (
        <div className="input">Input!</div>
    );
};

export default Input;